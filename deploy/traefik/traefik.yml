api:
  dashboard: false

log:
  level: INFO

# docref: begin safe-access-log
accessLog:
  format: json
  fields:
    names:
      # Traefik 3.7.1 cannot remove only the query component, so omit both
      # fields that contain the request URI. Method, host, status, timing,
      # router, service, and client metadata remain available.
      RequestPath: drop
      RequestLine: drop
    headers:
      defaultMode: drop
# docref: end safe-access-log

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: false

certificatesResolvers:
  letsencrypt:
    acme:
      storage: /letsencrypt/acme.json
      httpChallenge:
        entryPoint: web
