name: Tests

on:
  push:
    branches: [main, 'v*']
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'internal/**'
      - 'cmd/**'
      - '.github/workflows/test.yml'
  pull_request:
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'internal/**'
      - 'cmd/**'
      - '.github/workflows/test.yml'

jobs:
  unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      # go.mod pins a specific SDK commit via pseudo-version + replace
      # directive, so unit tests build against exactly that SDK state.
      # Bumping the pin is an explicit commit, not something that
      # happens silently when SDK main advances.
      #
      # Audit N020: the per-package list is intentionally explicit; the
      # split between unit and integration jobs is load-bearing because
      # integration tests bring up testcontainers (Postgres, Valkey) and
      # take 5–20× longer. When you add a new internal/<pkg>, drop it
      # in the right list. eventtypes/... is pure-Go (no DB, no network)
      # so it lives here.
      - name: Run unit tests
        run: go test -count=1 ./internal/auth/... ./internal/connection/... ./internal/middleware/... ./internal/taskqueue/... ./internal/config/... ./internal/mtls/... ./internal/crypto/... ./internal/eventtypes/...

  # Integration tests are sharded across parallel runners so wall-clock is
  # the slowest shard rather than the sum of all packages (#338). Each shard
  # brings up its own testcontainers, so a Docker-registry flake (or a
  # failure) only re-runs that one shard, not the whole suite. The `api`
  # package dominates and on CI a full run approaches the 30m timeout, so it
  # is split into two name-partitioned sub-shards (^Test[A-L] / the rest,
  # ~balanced) that run in parallel; the `run` filter is `.` (everything) for
  # the others. When you add a new internal/<pkg>, put it in the smallest
  # non-api shard.
  integration-shard:
    name: Integration Tests (${{ matrix.shard.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shard:
          - name: api-1
            packages: ./internal/api/...
            run: '^Test[A-L]'
          - name: api-2
            packages: ./internal/api/...
            run: '^Test[^A-L]'
          - name: store-projectors
            packages: ./internal/store/... ./internal/projectors/...
            run: '.'
          - name: handlers-services
            packages: ./internal/handler/... ./internal/search/... ./internal/control/... ./internal/gateway/... ./internal/scim/... ./internal/ca/... ./internal/idp/...
            run: '.'
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      # projectors/... is integration-tier because each listener test
      # spins up a Postgres testcontainer to wire the store + the
      # listener factory; covered here so the 23 listener files +
      # 14 sibling test files get exercised on every push.
      - name: Run integration tests (${{ matrix.shard.name }})
        run: go test -count=1 -timeout 30m -run '${{ matrix.shard.run }}' ${{ matrix.shard.packages }}

  # Stable aggregate check: branch protection requires "Integration Tests"
  # (the shard jobs carry per-shard suffixes), so this gate fails unless
  # every shard succeeded. `if: always()` so a shard failure still resolves
  # this gate (to failed) instead of leaving it skipped/pending.
  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: integration-shard
    if: always()
    steps:
      - name: Require all integration shards to pass
        run: |
          if [ "${{ needs.integration-shard.result }}" != "success" ]; then
            echo "One or more integration shards failed: ${{ needs.integration-shard.result }}"
            exit 1
          fi
          echo "All integration shards passed."
