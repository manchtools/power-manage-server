// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: assignments.sql

package generated

import (
	"context"
	"time"
)

const availableAssignmentExistsForDevice = `-- name: AvailableAssignmentExistsForDevice :one
SELECT EXISTS (
    SELECT 1
    FROM assignments a
    JOIN devices d ON d.id = ?1 AND d.is_deleted = FALSE
    WHERE a.is_deleted = FALSE
      AND a.mode = 1
      AND a.source_type = ?2
      AND a.source_id = ?3
      AND (
          (a.source_type = 'action' AND EXISTS (SELECT 1 FROM actions s WHERE s.id = a.source_id AND s.is_deleted = FALSE))
          OR (a.source_type = 'action_set' AND EXISTS (SELECT 1 FROM action_sets s WHERE s.id = a.source_id AND s.is_deleted = FALSE))
          OR (a.source_type = 'definition' AND EXISTS (SELECT 1 FROM definitions s WHERE s.id = a.source_id AND s.is_deleted = FALSE))
          OR (a.source_type = 'compliance_policy' AND EXISTS (SELECT 1 FROM compliance_policies s WHERE s.id = a.source_id AND s.is_deleted = FALSE))
      )
      AND (
          (a.target_type = 'device' AND a.target_id = d.id)
          OR (a.target_type = 'device_group' AND EXISTS (
              SELECT 1 FROM device_group_members dgm
              JOIN device_groups dg ON dg.id = dgm.group_id AND dg.is_deleted = FALSE
              WHERE dgm.device_id = d.id AND dgm.group_id = a.target_id
          ))
          OR (a.target_type = 'user' AND (
              EXISTS (SELECT 1 FROM device_assigned_users dau
                      JOIN users u ON u.id = dau.user_id AND u.is_deleted = FALSE
                      WHERE dau.device_id = d.id AND dau.user_id = a.target_id)
              OR EXISTS (SELECT 1 FROM device_assigned_groups dag
                         JOIN user_groups ug ON ug.id = dag.group_id AND ug.is_deleted = FALSE
                         JOIN user_group_members ugm ON ugm.group_id = dag.group_id
                         WHERE dag.device_id = d.id AND ugm.user_id = a.target_id)
          ))
          OR (a.target_type = 'user_group' AND (
              EXISTS (SELECT 1 FROM device_assigned_groups dag
                      JOIN user_groups ug ON ug.id = dag.group_id AND ug.is_deleted = FALSE
                      WHERE dag.device_id = d.id AND dag.group_id = a.target_id)
              OR EXISTS (SELECT 1 FROM device_assigned_users dau
                         JOIN users u ON u.id = dau.user_id AND u.is_deleted = FALSE
                         JOIN user_group_members ugm ON ugm.user_id = dau.user_id
                         JOIN user_groups ug ON ug.id = ugm.group_id AND ug.is_deleted = FALSE
                         WHERE dau.device_id = d.id AND ugm.group_id = a.target_id)
          ))
      )
)
`

type AvailableAssignmentExistsForDeviceParams struct {
	DeviceID   string `json:"device_id"`
	SourceType string `json:"source_type"`
	SourceID   string `json:"source_id"`
}

func (q *Queries) AvailableAssignmentExistsForDevice(ctx context.Context, arg AvailableAssignmentExistsForDeviceParams) (bool, error) {
	row := q.db.QueryRowContext(ctx, availableAssignmentExistsForDevice, arg.DeviceID, arg.SourceType, arg.SourceID)
	var exists bool
	err := row.Scan(&exists)
	return exists, err
}

const countAssignmentViews = `-- name: CountAssignmentViews :one
SELECT COUNT(*)
FROM assignments a
LEFT JOIN actions sa ON a.source_type = 'action' AND sa.id = a.source_id AND sa.is_deleted = FALSE
LEFT JOIN action_sets ss ON a.source_type = 'action_set' AND ss.id = a.source_id AND ss.is_deleted = FALSE
LEFT JOIN definitions sd ON a.source_type = 'definition' AND sd.id = a.source_id AND sd.is_deleted = FALSE
LEFT JOIN compliance_policies sp ON a.source_type = 'compliance_policy' AND sp.id = a.source_id AND sp.is_deleted = FALSE
LEFT JOIN devices td ON a.target_type = 'device' AND td.id = a.target_id AND td.is_deleted = FALSE
LEFT JOIN device_groups tdg ON a.target_type = 'device_group' AND tdg.id = a.target_id AND tdg.is_deleted = FALSE
LEFT JOIN users tu ON a.target_type = 'user' AND tu.id = a.target_id AND tu.is_deleted = FALSE
LEFT JOIN user_groups tug ON a.target_type = 'user_group' AND tug.id = a.target_id AND tug.is_deleted = FALSE
WHERE a.is_deleted = FALSE
  AND (?1 = '' OR a.source_type = ?1)
  AND (?2 = '' OR a.source_id = ?2)
  AND (?3 = '' OR a.target_type = ?3)
  AND (?4 = '' OR a.target_id = ?4)
  AND COALESCE(sa.id, ss.id, sd.id, sp.id) IS NOT NULL
  AND COALESCE(td.id, tdg.id, tu.id, tug.id) IS NOT NULL
`

type CountAssignmentViewsParams struct {
	SourceType interface{} `json:"source_type"`
	SourceID   interface{} `json:"source_id"`
	TargetType interface{} `json:"target_type"`
	TargetID   interface{} `json:"target_id"`
}

func (q *Queries) CountAssignmentViews(ctx context.Context, arg CountAssignmentViewsParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, countAssignmentViews,
		arg.SourceType,
		arg.SourceID,
		arg.TargetType,
		arg.TargetID,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getAssignmentByID = `-- name: GetAssignmentByID :one
SELECT a.id, a.source_type, a.source_id, a.target_type, a.target_id, a.sort_order, a.mode, a.created_at, a.created_by, a.is_deleted,
       COALESCE(sa.name, ss.name, sd.name, sp.name, '') AS resolved_source_name,
       COALESCE(td.hostname, tdg.name, tu.display_name, tug.name, '') AS resolved_target_name
FROM assignments a
LEFT JOIN actions sa ON a.source_type = 'action' AND sa.id = a.source_id AND sa.is_deleted = FALSE
LEFT JOIN action_sets ss ON a.source_type = 'action_set' AND ss.id = a.source_id AND ss.is_deleted = FALSE
LEFT JOIN definitions sd ON a.source_type = 'definition' AND sd.id = a.source_id AND sd.is_deleted = FALSE
LEFT JOIN compliance_policies sp ON a.source_type = 'compliance_policy' AND sp.id = a.source_id AND sp.is_deleted = FALSE
LEFT JOIN devices td ON a.target_type = 'device' AND td.id = a.target_id AND td.is_deleted = FALSE
LEFT JOIN device_groups tdg ON a.target_type = 'device_group' AND tdg.id = a.target_id AND tdg.is_deleted = FALSE
LEFT JOIN users tu ON a.target_type = 'user' AND tu.id = a.target_id AND tu.is_deleted = FALSE
LEFT JOIN user_groups tug ON a.target_type = 'user_group' AND tug.id = a.target_id AND tug.is_deleted = FALSE
WHERE a.id = ? AND a.is_deleted = FALSE
  AND COALESCE(sa.id, ss.id, sd.id, sp.id) IS NOT NULL
  AND COALESCE(td.id, tdg.id, tu.id, tug.id) IS NOT NULL
`

type GetAssignmentByIDRow struct {
	ID                 string     `json:"id"`
	SourceType         string     `json:"source_type"`
	SourceID           string     `json:"source_id"`
	TargetType         string     `json:"target_type"`
	TargetID           string     `json:"target_id"`
	SortOrder          int32      `json:"sort_order"`
	Mode               int32      `json:"mode"`
	CreatedAt          *time.Time `json:"created_at"`
	CreatedBy          string     `json:"created_by"`
	IsDeleted          bool       `json:"is_deleted"`
	ResolvedSourceName string     `json:"resolved_source_name"`
	ResolvedTargetName string     `json:"resolved_target_name"`
}

func (q *Queries) GetAssignmentByID(ctx context.Context, id string) (GetAssignmentByIDRow, error) {
	row := q.db.QueryRowContext(ctx, getAssignmentByID, id)
	var i GetAssignmentByIDRow
	err := row.Scan(
		&i.ID,
		&i.SourceType,
		&i.SourceID,
		&i.TargetType,
		&i.TargetID,
		&i.SortOrder,
		&i.Mode,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.IsDeleted,
		&i.ResolvedSourceName,
		&i.ResolvedTargetName,
	)
	return i, err
}

const getAssignmentByTuple = `-- name: GetAssignmentByTuple :one
SELECT a.id, a.source_type, a.source_id, a.target_type, a.target_id, a.sort_order, a.mode, a.created_at, a.created_by, a.is_deleted,
       COALESCE(sa.name, ss.name, sd.name, sp.name, '') AS source_name,
       COALESCE(td.hostname, tdg.name, tu.display_name, tug.name, '') AS target_name
FROM assignments a
LEFT JOIN actions sa ON a.source_type = 'action' AND sa.id = a.source_id AND sa.is_deleted = FALSE
LEFT JOIN action_sets ss ON a.source_type = 'action_set' AND ss.id = a.source_id AND ss.is_deleted = FALSE
LEFT JOIN definitions sd ON a.source_type = 'definition' AND sd.id = a.source_id AND sd.is_deleted = FALSE
LEFT JOIN compliance_policies sp ON a.source_type = 'compliance_policy' AND sp.id = a.source_id AND sp.is_deleted = FALSE
LEFT JOIN devices td ON a.target_type = 'device' AND td.id = a.target_id AND td.is_deleted = FALSE
LEFT JOIN device_groups tdg ON a.target_type = 'device_group' AND tdg.id = a.target_id AND tdg.is_deleted = FALSE
LEFT JOIN users tu ON a.target_type = 'user' AND tu.id = a.target_id AND tu.is_deleted = FALSE
LEFT JOIN user_groups tug ON a.target_type = 'user_group' AND tug.id = a.target_id AND tug.is_deleted = FALSE
WHERE a.source_type = ?1
  AND a.source_id = ?2
  AND a.target_type = ?3
  AND a.target_id = ?4
  AND a.is_deleted = FALSE
  AND COALESCE(sa.id, ss.id, sd.id, sp.id) IS NOT NULL
  AND COALESCE(td.id, tdg.id, tu.id, tug.id) IS NOT NULL
`

type GetAssignmentByTupleParams struct {
	SourceType string `json:"source_type"`
	SourceID   string `json:"source_id"`
	TargetType string `json:"target_type"`
	TargetID   string `json:"target_id"`
}

type GetAssignmentByTupleRow struct {
	ID         string     `json:"id"`
	SourceType string     `json:"source_type"`
	SourceID   string     `json:"source_id"`
	TargetType string     `json:"target_type"`
	TargetID   string     `json:"target_id"`
	SortOrder  int32      `json:"sort_order"`
	Mode       int32      `json:"mode"`
	CreatedAt  *time.Time `json:"created_at"`
	CreatedBy  string     `json:"created_by"`
	IsDeleted  bool       `json:"is_deleted"`
	SourceName string     `json:"source_name"`
	TargetName string     `json:"target_name"`
}

func (q *Queries) GetAssignmentByTuple(ctx context.Context, arg GetAssignmentByTupleParams) (GetAssignmentByTupleRow, error) {
	row := q.db.QueryRowContext(ctx, getAssignmentByTuple,
		arg.SourceType,
		arg.SourceID,
		arg.TargetType,
		arg.TargetID,
	)
	var i GetAssignmentByTupleRow
	err := row.Scan(
		&i.ID,
		&i.SourceType,
		&i.SourceID,
		&i.TargetType,
		&i.TargetID,
		&i.SortOrder,
		&i.Mode,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.IsDeleted,
		&i.SourceName,
		&i.TargetName,
	)
	return i, err
}

const getDeviceGroupID = `-- name: GetDeviceGroupID :one
SELECT id FROM device_groups WHERE id = ? AND is_deleted = FALSE
`

func (q *Queries) GetDeviceGroupID(ctx context.Context, id string) (string, error) {
	row := q.db.QueryRowContext(ctx, getDeviceGroupID, id)
	var id_2 string
	err := row.Scan(&id_2)
	return id_2, err
}

const listAssignmentViews = `-- name: ListAssignmentViews :many
SELECT a.id, a.source_type, a.source_id, a.target_type, a.target_id, a.sort_order, a.mode, a.created_at, a.created_by, a.is_deleted,
       COALESCE(sa.name, ss.name, sd.name, sp.name, '') AS resolved_source_name,
       COALESCE(td.hostname, tdg.name, tu.display_name, tug.name, '') AS resolved_target_name
FROM assignments a
LEFT JOIN actions sa ON a.source_type = 'action' AND sa.id = a.source_id AND sa.is_deleted = FALSE
LEFT JOIN action_sets ss ON a.source_type = 'action_set' AND ss.id = a.source_id AND ss.is_deleted = FALSE
LEFT JOIN definitions sd ON a.source_type = 'definition' AND sd.id = a.source_id AND sd.is_deleted = FALSE
LEFT JOIN compliance_policies sp ON a.source_type = 'compliance_policy' AND sp.id = a.source_id AND sp.is_deleted = FALSE
LEFT JOIN devices td ON a.target_type = 'device' AND td.id = a.target_id AND td.is_deleted = FALSE
LEFT JOIN device_groups tdg ON a.target_type = 'device_group' AND tdg.id = a.target_id AND tdg.is_deleted = FALSE
LEFT JOIN users tu ON a.target_type = 'user' AND tu.id = a.target_id AND tu.is_deleted = FALSE
LEFT JOIN user_groups tug ON a.target_type = 'user_group' AND tug.id = a.target_id AND tug.is_deleted = FALSE
WHERE a.is_deleted = FALSE
  AND a.id > ?1
  AND (?2 = '' OR a.source_type = ?2)
  AND (?3 = '' OR a.source_id = ?3)
  AND (?4 = '' OR a.target_type = ?4)
  AND (?5 = '' OR a.target_id = ?5)
  AND COALESCE(sa.id, ss.id, sd.id, sp.id) IS NOT NULL
  AND COALESCE(td.id, tdg.id, tu.id, tug.id) IS NOT NULL
ORDER BY a.id
LIMIT ?6
`

type ListAssignmentViewsParams struct {
	AfterID    string      `json:"after_id"`
	SourceType interface{} `json:"source_type"`
	SourceID   interface{} `json:"source_id"`
	TargetType interface{} `json:"target_type"`
	TargetID   interface{} `json:"target_id"`
	RowLimit   int64       `json:"row_limit"`
}

type ListAssignmentViewsRow struct {
	ID                 string     `json:"id"`
	SourceType         string     `json:"source_type"`
	SourceID           string     `json:"source_id"`
	TargetType         string     `json:"target_type"`
	TargetID           string     `json:"target_id"`
	SortOrder          int32      `json:"sort_order"`
	Mode               int32      `json:"mode"`
	CreatedAt          *time.Time `json:"created_at"`
	CreatedBy          string     `json:"created_by"`
	IsDeleted          bool       `json:"is_deleted"`
	ResolvedSourceName string     `json:"resolved_source_name"`
	ResolvedTargetName string     `json:"resolved_target_name"`
}

func (q *Queries) ListAssignmentViews(ctx context.Context, arg ListAssignmentViewsParams) ([]ListAssignmentViewsRow, error) {
	rows, err := q.db.QueryContext(ctx, listAssignmentViews,
		arg.AfterID,
		arg.SourceType,
		arg.SourceID,
		arg.TargetType,
		arg.TargetID,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAssignmentViewsRow{}
	for rows.Next() {
		var i ListAssignmentViewsRow
		if err := rows.Scan(
			&i.ID,
			&i.SourceType,
			&i.SourceID,
			&i.TargetType,
			&i.TargetID,
			&i.SortOrder,
			&i.Mode,
			&i.CreatedAt,
			&i.CreatedBy,
			&i.IsDeleted,
			&i.ResolvedSourceName,
			&i.ResolvedTargetName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAssignmentViewsForUser = `-- name: ListAssignmentViewsForUser :many
SELECT a.id, a.source_type, a.source_id, a.target_type, a.target_id, a.sort_order, a.mode, a.created_at, a.created_by, a.is_deleted,
       COALESCE(sa.name, ss.name, sd.name, sp.name, '') AS resolved_source_name,
       COALESCE(tu.display_name, tug.name, '') AS resolved_target_name
FROM assignments a
LEFT JOIN actions sa ON a.source_type = 'action' AND sa.id = a.source_id AND sa.is_deleted = FALSE
LEFT JOIN action_sets ss ON a.source_type = 'action_set' AND ss.id = a.source_id AND ss.is_deleted = FALSE
LEFT JOIN definitions sd ON a.source_type = 'definition' AND sd.id = a.source_id AND sd.is_deleted = FALSE
LEFT JOIN compliance_policies sp ON a.source_type = 'compliance_policy' AND sp.id = a.source_id AND sp.is_deleted = FALSE
LEFT JOIN users tu ON a.target_type = 'user' AND tu.id = a.target_id AND tu.is_deleted = FALSE
LEFT JOIN user_groups tug ON a.target_type = 'user_group' AND tug.id = a.target_id AND tug.is_deleted = FALSE
WHERE a.is_deleted = FALSE
  AND (
      (a.target_type = 'user' AND a.target_id = ?1)
      OR (
          a.target_type = 'user_group'
          AND EXISTS (
              SELECT 1
              FROM user_group_members ugm
              WHERE ugm.group_id = a.target_id AND ugm.user_id = ?1
          )
      )
  )
  AND COALESCE(sa.id, ss.id, sd.id, sp.id) IS NOT NULL
  AND COALESCE(tu.id, tug.id) IS NOT NULL
ORDER BY a.id
`

type ListAssignmentViewsForUserRow struct {
	ID                 string     `json:"id"`
	SourceType         string     `json:"source_type"`
	SourceID           string     `json:"source_id"`
	TargetType         string     `json:"target_type"`
	TargetID           string     `json:"target_id"`
	SortOrder          int32      `json:"sort_order"`
	Mode               int32      `json:"mode"`
	CreatedAt          *time.Time `json:"created_at"`
	CreatedBy          string     `json:"created_by"`
	IsDeleted          bool       `json:"is_deleted"`
	ResolvedSourceName string     `json:"resolved_source_name"`
	ResolvedTargetName string     `json:"resolved_target_name"`
}

// User-targeted assignments resolve through the current materialized group
// memberships. Dynamic group evaluation updates the same membership table, so
// this read needs no second query language or compatibility path.
func (q *Queries) ListAssignmentViewsForUser(ctx context.Context, userID string) ([]ListAssignmentViewsForUserRow, error) {
	rows, err := q.db.QueryContext(ctx, listAssignmentViewsForUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAssignmentViewsForUserRow{}
	for rows.Next() {
		var i ListAssignmentViewsForUserRow
		if err := rows.Scan(
			&i.ID,
			&i.SourceType,
			&i.SourceID,
			&i.TargetType,
			&i.TargetID,
			&i.SortOrder,
			&i.Mode,
			&i.CreatedAt,
			&i.CreatedBy,
			&i.IsDeleted,
			&i.ResolvedSourceName,
			&i.ResolvedTargetName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listResolvedAssignmentSourcesForDevice = `-- name: ListResolvedAssignmentSourcesForDevice :many
WITH resolved_sources AS (
    SELECT DISTINCT a.source_type, a.source_id, a.mode
    FROM assignments a
    JOIN devices d ON d.id = ?1 AND d.is_deleted = FALSE
    WHERE a.is_deleted = FALSE
      AND (
          (a.target_type = 'device' AND a.target_id = d.id)
          OR (a.target_type = 'device_group' AND EXISTS (
              SELECT 1 FROM device_group_members dgm
              JOIN device_groups dg ON dg.id = dgm.group_id AND dg.is_deleted = FALSE
              WHERE dgm.device_id = d.id AND dgm.group_id = a.target_id
          ))
          OR (a.target_type = 'user' AND (
              EXISTS (SELECT 1 FROM device_assigned_users dau
                      JOIN users u ON u.id = dau.user_id AND u.is_deleted = FALSE
                      WHERE dau.device_id = d.id AND dau.user_id = a.target_id)
              OR EXISTS (SELECT 1 FROM device_assigned_groups dag
                         JOIN user_groups ug ON ug.id = dag.group_id AND ug.is_deleted = FALSE
                         JOIN user_group_members ugm ON ugm.group_id = dag.group_id
                         WHERE dag.device_id = d.id AND ugm.user_id = a.target_id)
          ))
          OR (a.target_type = 'user_group' AND (
              EXISTS (SELECT 1 FROM device_assigned_groups dag
                      JOIN user_groups ug ON ug.id = dag.group_id AND ug.is_deleted = FALSE
                      WHERE dag.device_id = d.id AND dag.group_id = a.target_id)
              OR EXISTS (SELECT 1 FROM device_assigned_users dau
                         JOIN users u ON u.id = dau.user_id AND u.is_deleted = FALSE
                         JOIN user_group_members ugm ON ugm.user_id = dau.user_id
                         JOIN user_groups ug ON ug.id = ugm.group_id AND ug.is_deleted = FALSE
                         WHERE dau.device_id = d.id AND ugm.group_id = a.target_id)
          ))
      )
)
SELECT resolved_sources.source_type,
       resolved_sources.source_id,
       resolved_sources.mode,
       COALESCE(sa.name, ss.name, sd.name, sp.name, '') AS source_name,
       COALESCE(sa.description, ss.description, sd.description, sp.description, '') AS source_description,
       COALESCE(us.selected, FALSE) AS selected
FROM resolved_sources
LEFT JOIN actions sa ON resolved_sources.source_type = 'action' AND sa.id = resolved_sources.source_id AND sa.is_deleted = FALSE
LEFT JOIN action_sets ss ON resolved_sources.source_type = 'action_set' AND ss.id = resolved_sources.source_id AND ss.is_deleted = FALSE
LEFT JOIN definitions sd ON resolved_sources.source_type = 'definition' AND sd.id = resolved_sources.source_id AND sd.is_deleted = FALSE
LEFT JOIN compliance_policies sp ON resolved_sources.source_type = 'compliance_policy' AND sp.id = resolved_sources.source_id AND sp.is_deleted = FALSE
LEFT JOIN user_selections us ON us.device_id = ?1
    AND us.source_type = resolved_sources.source_type AND us.source_id = resolved_sources.source_id
WHERE COALESCE(sa.id, ss.id, sd.id, sp.id) IS NOT NULL
ORDER BY resolved_sources.source_type, resolved_sources.source_id, resolved_sources.mode
`

type ListResolvedAssignmentSourcesForDeviceRow struct {
	SourceType        string `json:"source_type"`
	SourceID          string `json:"source_id"`
	Mode              int64  `json:"mode"`
	SourceName        string `json:"source_name"`
	SourceDescription string `json:"source_description"`
	Selected          bool   `json:"selected"`
}

func (q *Queries) ListResolvedAssignmentSourcesForDevice(ctx context.Context, deviceID string) ([]ListResolvedAssignmentSourcesForDeviceRow, error) {
	rows, err := q.db.QueryContext(ctx, listResolvedAssignmentSourcesForDevice, deviceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListResolvedAssignmentSourcesForDeviceRow{}
	for rows.Next() {
		var i ListResolvedAssignmentSourcesForDeviceRow
		if err := rows.Scan(
			&i.SourceType,
			&i.SourceID,
			&i.Mode,
			&i.SourceName,
			&i.SourceDescription,
			&i.Selected,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const softDeleteAssignment = `-- name: SoftDeleteAssignment :one
UPDATE assignments SET is_deleted = TRUE
WHERE id = ? AND is_deleted = FALSE
RETURNING id, source_type, source_id, target_type, target_id, sort_order, mode, created_at, created_by, is_deleted
`

func (q *Queries) SoftDeleteAssignment(ctx context.Context, id string) (Assignment, error) {
	row := q.db.QueryRowContext(ctx, softDeleteAssignment, id)
	var i Assignment
	err := row.Scan(
		&i.ID,
		&i.SourceType,
		&i.SourceID,
		&i.TargetType,
		&i.TargetID,
		&i.SortOrder,
		&i.Mode,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.IsDeleted,
	)
	return i, err
}

const upsertAssignment = `-- name: UpsertAssignment :one
INSERT INTO assignments (
    id, source_type, source_id, target_type, target_id, mode, created_at, created_by
)
VALUES (
    ?1, ?2, ?3,
    ?4, ?5, ?6,
    ?7, ?8
)
ON CONFLICT (source_type, source_id, target_type, target_id) DO UPDATE
SET is_deleted = FALSE,
    mode = EXCLUDED.mode,
    created_at = EXCLUDED.created_at,
    created_by = EXCLUDED.created_by
WHERE assignments.is_deleted = TRUE
RETURNING id, source_type, source_id, target_type, target_id, sort_order, mode, created_at, created_by, is_deleted
`

type UpsertAssignmentParams struct {
	ID         string     `json:"id"`
	SourceType string     `json:"source_type"`
	SourceID   string     `json:"source_id"`
	TargetType string     `json:"target_type"`
	TargetID   string     `json:"target_id"`
	Mode       int32      `json:"mode"`
	CreatedAt  *time.Time `json:"created_at"`
	CreatedBy  string     `json:"created_by"`
}

func (q *Queries) UpsertAssignment(ctx context.Context, arg UpsertAssignmentParams) (Assignment, error) {
	row := q.db.QueryRowContext(ctx, upsertAssignment,
		arg.ID,
		arg.SourceType,
		arg.SourceID,
		arg.TargetType,
		arg.TargetID,
		arg.Mode,
		arg.CreatedAt,
		arg.CreatedBy,
	)
	var i Assignment
	err := row.Scan(
		&i.ID,
		&i.SourceType,
		&i.SourceID,
		&i.TargetType,
		&i.TargetID,
		&i.SortOrder,
		&i.Mode,
		&i.CreatedAt,
		&i.CreatedBy,
		&i.IsDeleted,
	)
	return i, err
}

const upsertUserSelection = `-- name: UpsertUserSelection :one
INSERT INTO user_selections (
    id, device_id, source_type, source_id, selected, updated_at, created_by
) VALUES (
    ?1, ?2, ?3, ?4,
    ?5, ?6, ?7
)
ON CONFLICT (device_id, source_type, source_id) DO UPDATE
SET selected = EXCLUDED.selected,
    updated_at = EXCLUDED.updated_at,
    created_by = EXCLUDED.created_by
RETURNING id, device_id, source_type, source_id, selected, updated_at, created_by
`

type UpsertUserSelectionParams struct {
	ID         string    `json:"id"`
	DeviceID   string    `json:"device_id"`
	SourceType string    `json:"source_type"`
	SourceID   string    `json:"source_id"`
	Selected   bool      `json:"selected"`
	UpdatedAt  time.Time `json:"updated_at"`
	CreatedBy  string    `json:"created_by"`
}

func (q *Queries) UpsertUserSelection(ctx context.Context, arg UpsertUserSelectionParams) (UserSelection, error) {
	row := q.db.QueryRowContext(ctx, upsertUserSelection,
		arg.ID,
		arg.DeviceID,
		arg.SourceType,
		arg.SourceID,
		arg.Selected,
		arg.UpdatedAt,
		arg.CreatedBy,
	)
	var i UserSelection
	err := row.Scan(
		&i.ID,
		&i.DeviceID,
		&i.SourceType,
		&i.SourceID,
		&i.Selected,
		&i.UpdatedAt,
		&i.CreatedBy,
	)
	return i, err
}
