// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: reach.sql

package generated

import (
	"context"
)

const listMaintenanceWindowsForDevice = `-- name: ListMaintenanceWindowsForDevice :many
SELECT g.maintenance_window
FROM device_groups_projection g
JOIN device_group_members_projection m ON m.group_id = g.id
WHERE m.device_id = $1
  AND g.is_deleted = FALSE
  AND g.maintenance_window <> '{}'::JSONB
  AND g.maintenance_window IS NOT NULL

UNION ALL

SELECT ug.maintenance_window
FROM user_groups_projection ug
JOIN device_assigned_groups_projection dag ON dag.group_id = ug.id
WHERE dag.device_id = $1
  AND ug.is_deleted = FALSE
  AND ug.maintenance_window <> '{}'::JSONB
  AND ug.maintenance_window IS NOT NULL
`

// All non-empty maintenance windows reaching the device. The agent
// ORs entries within and across these (most-permissive union); the
// server just returns the raw set so the resolution layer can compute
// the union without committing to a single SQL aggregate.
//
// We include device groups the device is a member of (direct or via
// dynamic-membership rebuilds) and user groups directly assigned to
// the device. An empty schedule (the default) contributes nothing —
// the union of "constraint + no-constraint" stays "constraint",
// matching the per-group "empty = always allowed" semantics.
func (q *Queries) ListMaintenanceWindowsForDevice(ctx context.Context, deviceID string) ([][]byte, error) {
	rows, err := q.db.Query(ctx, listMaintenanceWindowsForDevice, deviceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := [][]byte{}
	for rows.Next() {
		var maintenance_window []byte
		if err := rows.Scan(&maintenance_window); err != nil {
			return nil, err
		}
		items = append(items, maintenance_window)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listReachedActionAssignmentsForDevice = `-- name: ListReachedActionAssignmentsForDevice :many
SELECT
  a.id,
  (CASE
    WHEN bool_or(asn.mode = 2) THEN 2
    WHEN bool_or(asn.mode = 3) THEN 3
    WHEN bool_or(asn.mode = 0) THEN 0
    ELSE 1
  END)::INTEGER AS effective_mode
FROM actions_projection a
JOIN (
  SELECT asn.source_id, asn.mode
    FROM assignments_projection asn
   WHERE asn.source_type = 'action'
     AND asn.target_type = 'device'
     AND asn.target_id   = $1
     AND asn.is_deleted  = FALSE

  UNION ALL

  SELECT asn.source_id, asn.mode
    FROM assignments_projection asn
    JOIN device_group_members_projection m
      ON m.group_id   = asn.target_id
     AND m.device_id  = $1
   WHERE asn.source_type = 'action'
     AND asn.target_type = 'device_group'
     AND asn.is_deleted  = FALSE
) asn ON asn.source_id = a.id
WHERE a.is_deleted = FALSE
GROUP BY a.id
ORDER BY a.id
`

type ListReachedActionAssignmentsForDeviceRow struct {
	ID            string `json:"id"`
	EffectiveMode int32  `json:"effective_mode"`
}

func (q *Queries) ListReachedActionAssignmentsForDevice(ctx context.Context, targetID string) ([]ListReachedActionAssignmentsForDeviceRow, error) {
	rows, err := q.db.Query(ctx, listReachedActionAssignmentsForDevice, targetID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListReachedActionAssignmentsForDeviceRow{}
	for rows.Next() {
		var i ListReachedActionAssignmentsForDeviceRow
		if err := rows.Scan(&i.ID, &i.EffectiveMode); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listReachedActionSetsForDevice = `-- name: ListReachedActionSetsForDevice :many
SELECT
  s.id,
  s.name,
  s.schedule,
  (CASE
    WHEN bool_or(asn.mode = 2) THEN 2
    WHEN bool_or(asn.mode = 3) THEN 3
    WHEN bool_or(asn.mode = 0) THEN 0
    ELSE 1
  END)::INTEGER AS effective_mode
FROM action_sets_projection s
JOIN (
  SELECT a.source_id, a.mode
    FROM assignments_projection a
   WHERE a.source_type = 'action_set'
     AND a.target_type = 'device'
     AND a.target_id   = $1
     AND a.is_deleted  = FALSE

  UNION ALL

  SELECT a.source_id, a.mode
    FROM assignments_projection a
    JOIN device_group_members_projection m
      ON m.group_id   = a.target_id
     AND m.device_id  = $1
   WHERE a.source_type = 'action_set'
     AND a.target_type = 'device_group'
     AND a.is_deleted  = FALSE
) asn ON asn.source_id = s.id
WHERE s.is_deleted = FALSE
GROUP BY s.id, s.name, s.schedule
ORDER BY s.name, s.id
`

type ListReachedActionSetsForDeviceRow struct {
	ID            string `json:"id"`
	Name          string `json:"name"`
	Schedule      []byte `json:"schedule"`
	EffectiveMode int32  `json:"effective_mode"`
}

func (q *Queries) ListReachedActionSetsForDevice(ctx context.Context, targetID string) ([]ListReachedActionSetsForDeviceRow, error) {
	rows, err := q.db.Query(ctx, listReachedActionSetsForDevice, targetID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListReachedActionSetsForDeviceRow{}
	for rows.Next() {
		var i ListReachedActionSetsForDeviceRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Schedule,
			&i.EffectiveMode,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listReachedDefinitionsForDevice = `-- name: ListReachedDefinitionsForDevice :many

SELECT
  d.id,
  d.name,
  d.schedule,
  (CASE
    WHEN bool_or(asn.mode = 2) THEN 2
    WHEN bool_or(asn.mode = 3) THEN 3
    WHEN bool_or(asn.mode = 0) THEN 0
    ELSE 1
  END)::INTEGER AS effective_mode
FROM definitions_projection d
JOIN (
  -- Directly assigned to this device
  SELECT a.source_id, a.mode
    FROM assignments_projection a
   WHERE a.source_type = 'definition'
     AND a.target_type = 'device'
     AND a.target_id   = $1
     AND a.is_deleted  = FALSE

  UNION ALL

  -- Assigned to a device group this device belongs to
  SELECT a.source_id, a.mode
    FROM assignments_projection a
    JOIN device_group_members_projection m
      ON m.group_id   = a.target_id
     AND m.device_id  = $1
   WHERE a.source_type = 'definition'
     AND a.target_type = 'device_group'
     AND a.is_deleted  = FALSE
) asn ON asn.source_id = d.id
WHERE d.is_deleted = FALSE
GROUP BY d.id, d.name, d.schedule
ORDER BY d.name, d.id
`

type ListReachedDefinitionsForDeviceRow struct {
	ID            string `json:"id"`
	Name          string `json:"name"`
	Schedule      []byte `json:"schedule"`
	EffectiveMode int32  `json:"effective_mode"`
}

// Reach queries for the agent sync tree.
//
// Implements the layered precedence introduced for the grouped-execution
// sync (manchtools/power-manage-agent#45):
//
//	definition (highest)  >  action_set  >  action (lowest)
//
// For each layer, we collapse the assignment modes on the rows that
// actually reach the device (directly or via device-group membership)
// using EXCLUDED > UNINSTALL > REQUIRED > AVAILABLE — same priority
// as the existing per-action resolver, just applied at the container
// level.
//
// A set is excluded from the standalone-set layer if it is a member
// of any reached definition (the definition absorbs it). An action is
// excluded from the standalone-action layer if it is a member of any
// reached set, definition-bound or standalone (the set absorbs it).
// The Go-side resolver enforces the absorb rules over the UNION of
// these queries.
func (q *Queries) ListReachedDefinitionsForDevice(ctx context.Context, targetID string) ([]ListReachedDefinitionsForDeviceRow, error) {
	rows, err := q.db.Query(ctx, listReachedDefinitionsForDevice, targetID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListReachedDefinitionsForDeviceRow{}
	for rows.Next() {
		var i ListReachedDefinitionsForDeviceRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Schedule,
			&i.EffectiveMode,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
