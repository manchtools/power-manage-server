// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: executions.sql

package generated

import (
	"context"
	"time"
)

const insertExecutionCreatedProjection = `-- name: InsertExecutionCreatedProjection :exec

INSERT INTO executions_projection (
    id, device_id, action_id, action_type, desired_state,
    params, timeout_seconds, status, created_at,
    created_by_type, created_by_id, projection_version
) VALUES ($1, $2, $3, $4, $5, $6, $7, 'pending', $8, $9, $10, $11)
ON CONFLICT (id) DO NOTHING
`

type InsertExecutionCreatedProjectionParams struct {
	ID                string     `json:"id"`
	DeviceID          string     `json:"device_id"`
	ActionID          *string    `json:"action_id"`
	ActionType        int32      `json:"action_type"`
	DesiredState      int32      `json:"desired_state"`
	Params            []byte     `json:"params"`
	TimeoutSeconds    int32      `json:"timeout_seconds"`
	CreatedAt         *time.Time `json:"created_at"`
	CreatedByType     string     `json:"created_by_type"`
	CreatedByID       string     `json:"created_by_id"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ============================================================================
// Projector listener writes (manchtools/power-manage-server#136).
// ============================================================================
//
// Mirrors the deleted PL/pgSQL project_execution_event(). Nine event
// types, every one a single-statement write against
// executions_projection: 2 INSERTs (Created, Scheduled) plus 7 UPDATEs
// (Dispatched, Started, Completed, Failed, TimedOut, Skipped,
// Cancelled). No cross-stream effects, no member tables, no compliance
// cascade — the simplest port left under tracker #136.
//
// Tightening vs the PL/pgSQL projector: every UPDATE carries an
// explicit `WHERE projection_version < $N` guard and uses :execrows so
// the listener can short-circuit on stale-replay (asymmetric-guard
// discipline). The PL/pgSQL projector stamped projection_version
// without a guard, so an out-of-order Completed re-applied after a
// newer Failed would silently rewind the row's terminal status.
//
// ExecutionCancelled keeps the PL/pgSQL `AND status IN ('scheduled',
// 'pending')` business-logic guard verbatim alongside the new
// projection_version guard — a cancel arriving after the dispatch has
// fired is still a documented no-op, and stale replays on top of that
// still no-op via the version guard.
// ExecutionCreated handler. Mirrors the PL/pgSQL
// INSERT INTO executions_projection (...) VALUES (...) — status is
// hardcoded to 'pending', created_at falls back to event.occurred_at
// when the payload omits executed_at. ON CONFLICT DO NOTHING for
// replay safety.
func (q *Queries) InsertExecutionCreatedProjection(ctx context.Context, arg InsertExecutionCreatedProjectionParams) error {
	_, err := q.db.Exec(ctx, insertExecutionCreatedProjection,
		arg.ID,
		arg.DeviceID,
		arg.ActionID,
		arg.ActionType,
		arg.DesiredState,
		arg.Params,
		arg.TimeoutSeconds,
		arg.CreatedAt,
		arg.CreatedByType,
		arg.CreatedByID,
		arg.ProjectionVersion,
	)
	return err
}

const insertExecutionScheduledProjection = `-- name: InsertExecutionScheduledProjection :exec
INSERT INTO executions_projection (
    id, device_id, action_id, action_type, desired_state,
    params, timeout_seconds, status, scheduled_for, created_at,
    created_by_type, created_by_id, projection_version
) VALUES ($1, $2, $3, $4, $5, $6, $7, 'scheduled', $8, $9, $10, $11, $12)
ON CONFLICT (id) DO NOTHING
`

type InsertExecutionScheduledProjectionParams struct {
	ID                string     `json:"id"`
	DeviceID          string     `json:"device_id"`
	ActionID          *string    `json:"action_id"`
	ActionType        int32      `json:"action_type"`
	DesiredState      int32      `json:"desired_state"`
	Params            []byte     `json:"params"`
	TimeoutSeconds    int32      `json:"timeout_seconds"`
	ScheduledFor      *time.Time `json:"scheduled_for"`
	CreatedAt         *time.Time `json:"created_at"`
	CreatedByType     string     `json:"created_by_type"`
	CreatedByID       string     `json:"created_by_id"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ExecutionScheduled handler. Same column shape as the Created path
// plus scheduled_for; status is hardcoded to 'scheduled'. created_at
// always uses event.occurred_at on this branch (no executed_at
// fallback in the PL/pgSQL source). ON CONFLICT DO NOTHING for replay
// safety.
func (q *Queries) InsertExecutionScheduledProjection(ctx context.Context, arg InsertExecutionScheduledProjectionParams) error {
	_, err := q.db.Exec(ctx, insertExecutionScheduledProjection,
		arg.ID,
		arg.DeviceID,
		arg.ActionID,
		arg.ActionType,
		arg.DesiredState,
		arg.Params,
		arg.TimeoutSeconds,
		arg.ScheduledFor,
		arg.CreatedAt,
		arg.CreatedByType,
		arg.CreatedByID,
		arg.ProjectionVersion,
	)
	return err
}

const updateExecutionCancelledProjection = `-- name: UpdateExecutionCancelledProjection :execrows
UPDATE executions_projection
SET status             = 'cancelled',
    completed_at       = $2,
    error              = $3,
    projection_version = $4
WHERE id = $1
  AND projection_version < $4
  AND status IN ('scheduled', 'pending')
`

type UpdateExecutionCancelledProjectionParams struct {
	ID                string     `json:"id"`
	CompletedAt       *time.Time `json:"completed_at"`
	Error             *string    `json:"error"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ExecutionCancelled handler. The PL/pgSQL projector layered TWO
// guards: the status whitelist (only flip rows still in 'scheduled'
// or 'pending' so a cancel arriving after dispatch is a documented
// no-op) and now the projection_version guard (so a stale cancel
// replayed by the reconciler cannot rewind a row that has since moved
// on). Both guards stay — the status guard is business logic; the
// version guard is replay safety.
func (q *Queries) UpdateExecutionCancelledProjection(ctx context.Context, arg UpdateExecutionCancelledProjectionParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateExecutionCancelledProjection,
		arg.ID,
		arg.CompletedAt,
		arg.Error,
		arg.ProjectionVersion,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateExecutionCompletedProjection = `-- name: UpdateExecutionCompletedProjection :execrows
UPDATE executions_projection
SET status             = 'success',
    completed_at       = $2,
    output             = $3,
    duration_ms        = $4,
    changed            = $5,
    compliant          = $6,
    detection_output   = $7,
    projection_version = $8
WHERE id = $1
  AND projection_version < $8
`

type UpdateExecutionCompletedProjectionParams struct {
	ID                string     `json:"id"`
	CompletedAt       *time.Time `json:"completed_at"`
	Output            []byte     `json:"output"`
	DurationMs        *int64     `json:"duration_ms"`
	Changed           bool       `json:"changed"`
	Compliant         bool       `json:"compliant"`
	DetectionOutput   []byte     `json:"detection_output"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ExecutionCompleted handler. Mirrors the PL/pgSQL terminal-success
// branch — status='success', plus the full result-payload column set
// (output, duration_ms, changed, compliant, detection_output). The
// projection_version guard prevents an older Completed from clobbering
// a newer terminal state (Failed/TimedOut/etc).
func (q *Queries) UpdateExecutionCompletedProjection(ctx context.Context, arg UpdateExecutionCompletedProjectionParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateExecutionCompletedProjection,
		arg.ID,
		arg.CompletedAt,
		arg.Output,
		arg.DurationMs,
		arg.Changed,
		arg.Compliant,
		arg.DetectionOutput,
		arg.ProjectionVersion,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateExecutionDispatchedProjection = `-- name: UpdateExecutionDispatchedProjection :execrows
UPDATE executions_projection
SET status             = 'dispatched',
    dispatched_at      = $2,
    projection_version = $3
WHERE id = $1
  AND projection_version < $3
`

type UpdateExecutionDispatchedProjectionParams struct {
	ID                string     `json:"id"`
	DispatchedAt      *time.Time `json:"dispatched_at"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ExecutionDispatched handler. Single UPDATE, projection_version
// guard. :execrows so the listener can log + short-circuit on stale
// replay (no downstream cascade exists, but :execrows keeps the shape
// uniform across every UPDATE handler in this projector).
func (q *Queries) UpdateExecutionDispatchedProjection(ctx context.Context, arg UpdateExecutionDispatchedProjectionParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateExecutionDispatchedProjection, arg.ID, arg.DispatchedAt, arg.ProjectionVersion)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateExecutionFailedProjection = `-- name: UpdateExecutionFailedProjection :execrows
UPDATE executions_projection
SET status             = 'failed',
    completed_at       = $2,
    error              = $3,
    output             = $4,
    duration_ms        = $5,
    changed            = $6,
    compliant          = $7,
    detection_output   = $8,
    projection_version = $9
WHERE id = $1
  AND projection_version < $9
`

type UpdateExecutionFailedProjectionParams struct {
	ID                string     `json:"id"`
	CompletedAt       *time.Time `json:"completed_at"`
	Error             *string    `json:"error"`
	Output            []byte     `json:"output"`
	DurationMs        *int64     `json:"duration_ms"`
	Changed           bool       `json:"changed"`
	Compliant         bool       `json:"compliant"`
	DetectionOutput   []byte     `json:"detection_output"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ExecutionFailed handler. Same shape as Completed plus error.
func (q *Queries) UpdateExecutionFailedProjection(ctx context.Context, arg UpdateExecutionFailedProjectionParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateExecutionFailedProjection,
		arg.ID,
		arg.CompletedAt,
		arg.Error,
		arg.Output,
		arg.DurationMs,
		arg.Changed,
		arg.Compliant,
		arg.DetectionOutput,
		arg.ProjectionVersion,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateExecutionSkippedProjection = `-- name: UpdateExecutionSkippedProjection :execrows
UPDATE executions_projection
SET status             = 'skipped',
    completed_at       = $2,
    error              = $3,
    projection_version = $4
WHERE id = $1
  AND projection_version < $4
`

type UpdateExecutionSkippedProjectionParams struct {
	ID                string     `json:"id"`
	CompletedAt       *time.Time `json:"completed_at"`
	Error             *string    `json:"error"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ExecutionSkipped handler. completed_at is event.occurred_at (no
// payload fallback in the PL/pgSQL source); error column carries the
// skip reason.
func (q *Queries) UpdateExecutionSkippedProjection(ctx context.Context, arg UpdateExecutionSkippedProjectionParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateExecutionSkippedProjection,
		arg.ID,
		arg.CompletedAt,
		arg.Error,
		arg.ProjectionVersion,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateExecutionStartedProjection = `-- name: UpdateExecutionStartedProjection :execrows
UPDATE executions_projection
SET status             = 'running',
    started_at         = $2,
    projection_version = $3
WHERE id = $1
  AND projection_version < $3
`

type UpdateExecutionStartedProjectionParams struct {
	ID                string     `json:"id"`
	StartedAt         *time.Time `json:"started_at"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ExecutionStarted handler. Single UPDATE, projection_version guard.
func (q *Queries) UpdateExecutionStartedProjection(ctx context.Context, arg UpdateExecutionStartedProjectionParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateExecutionStartedProjection, arg.ID, arg.StartedAt, arg.ProjectionVersion)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateExecutionTimedOutProjection = `-- name: UpdateExecutionTimedOutProjection :execrows
UPDATE executions_projection
SET status             = 'timeout',
    completed_at       = $2,
    error              = $3,
    output             = $4,
    duration_ms        = $5,
    projection_version = $6
WHERE id = $1
  AND projection_version < $6
`

type UpdateExecutionTimedOutProjectionParams struct {
	ID                string     `json:"id"`
	CompletedAt       *time.Time `json:"completed_at"`
	Error             *string    `json:"error"`
	Output            []byte     `json:"output"`
	DurationMs        *int64     `json:"duration_ms"`
	ProjectionVersion int64      `json:"projection_version"`
}

// ExecutionTimedOut handler. Subset of the Failed shape — no
// changed / compliant / detection_output (the PL/pgSQL projector did
// not write those columns on timeout, leaving the column defaults to
// stand for fresh rows and preserving prior values for in-flight
// replays).
func (q *Queries) UpdateExecutionTimedOutProjection(ctx context.Context, arg UpdateExecutionTimedOutProjectionParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateExecutionTimedOutProjection,
		arg.ID,
		arg.CompletedAt,
		arg.Error,
		arg.Output,
		arg.DurationMs,
		arg.ProjectionVersion,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
